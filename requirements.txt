# Dependency versions are declared once, in pyproject.toml. This file is only a
# convenience for `pip install -r requirements.txt` on a dev checkout; it pulls
# the runtime deps plus the Streamlit UI and test tooling.
-e .[app,dev]
